{
  "id": "the-eternal-skald",
  "title": "The Eternal Skald",
  "description": "<p>v0.13.0 — <strong>Journeys that arrive when they should</strong>. Fixes the long-standing immersion break where the Skald narrated <strong>arriving at a destination while the journey track was barely filled</strong> (e.g. 2/10). The mechanics were always correct — this release adds <strong>progress-percentage-aware narrative pacing</strong> so the storyteller stays in step with the track. After every <strong>Undertake a Journey</strong> the Skald now receives banded guidance (<em>just begun → well underway → nearing the end → all but charted</em>) instructing it to treat each waypoint as a <strong>dramatic beat, not a geographic milestone</strong>, and to <strong>withhold arrival</strong> until the track is nearly full — arrival itself is resolved only by the <strong>Reach Your Destination</strong> progress roll. A previously-silent <strong>miss</strong> on a journey is now handled true to the rules: <strong>no progress is marked</strong> and the party is narrated as halted by an obstacle they must overcome. A new permanent <strong>JOURNEY PACING</strong> doctrine in the AI prompt keeps this consistent even during manual narration, and <strong>progress %</strong> is now surfaced in the UI — <code>!progress</code> lists each journey as <em>X/10 (Y%)</em> with a pacing hint, and progress toasts show the percentage. Purely additive and backwards-compatible — no data-model, settings or API changes — and covered by the full regression suite.</p><p>v0.12.0 — <strong>Delve into sites the Skald discovers</strong>. A new <strong>Discover a Site</strong> feature brings Ironsworn: Delve to the table. The Skald rolls a site&#39;s <strong>theme and domain</strong> on the official Delve oracles, then asks the AI to author an evocative site — name, summary, rank, features/dangers and <strong>three unanswered questions</strong> (<em>mystery, not explanation</em>) — posts it as a saga card, opens a <strong>delve progress track</strong> for it, and inscribes the full details into the Living Chronicle. When the AI is disabled or unreachable it falls back to a <strong>deterministic oracle-built site</strong>, so it always produces a usable result. The two Delve progress moves are now wired up: <strong>Locate Your Objective</strong> and <strong>Escape the Depths</strong> roll against your <strong>site&#39;s progress score</strong> — the Skald uses your one open site automatically, asks you to <strong>choose</strong> when several are open, and points you to <em>Discover a Site</em> first when none are. Also fixes <strong>browser AI memory (RAG) failing on plain-HTTP Foundry</strong>: the embedding cache is only used in a <strong>secure context</strong> (HTTPS/localhost) and otherwise falls back to in-memory, so semantic memory no longer hard-fails with «Browser cache is not available» on a LAN/remote host. Purely additive, backwards-compatible, and covered by new regression tests.</p><p>v0.11.3 — <strong>Journeys named for where you&#39;re actually going</strong>. Auto-created journey tracks are no longer all called «The Journey» — a new <strong>multi-layer destination resolver</strong> reads the place you named and titles the track for it. When you declare a move it now keeps your <strong>original words</strong> (e.g. <code>!Undertake a Journey to Ironhome</code> → <em>Journey to Ironhome</em>) instead of discarding them; a broadened, case-insensitive pattern understands far more phrasings (<em>set out for</em>, <em>make for</em>, <em>bound for</em>, <em>sail to</em>, <em>march on</em>, bare compass directions like <em>journey north</em>, …) and trims trailing clauses so only the place name remains; it then <strong>matches real places already on your map</strong> (scene name &amp; journal-pin locations) for correct spelling, and for poetic phrasing can ask a small, bounded, <strong>optional AI</strong> pass (new <em>AI Journey Destination Naming</em> setting, default on) to extract the destination. When nothing names a place it falls back to your <strong>active vow or current scene</strong> rather than a bare repeated label. Also: <strong>Reach Your Destination</strong> is now <strong>gated behind real progress</strong> — by default a journey needs at least <strong>4 filled progress boxes</strong> before the progress roll is allowed (configurable via two new settings: <em>Require Progress Before Reaching a Destination</em> and <em>Minimum Progress Boxes</em>), mirroring how Ironsworn is played. A new <code>!progress</code> command lets you <strong>review your open journeys</strong> or <strong>mark travel progress by hand</strong> (<code>!progress 2</code>, or <code>!progress 2 &lt;journey name&gt;</code>). Purely additive, backwards-compatible, defensive throughout, and covered by new naming tests.</p><p>v0.11.2 — <strong>Two UI fixes: clean scouting cards &amp; wrapping chronicle lists</strong>. Fixed the <code>!scout</code> command sometimes dumping a wall of <strong>raw JSON</strong> into chat: when the vision model&#39;s reply was cut off by the token limit (an unterminated JSON object), the parser fell back to showing the raw text. It now <strong>salvages the readable fields</strong> (summary, terrain and whatever map labels were fully transcribed) from truncated or malformed JSON, so the Scouting card always renders cleanly. Also fixed long entry names in the <code>!journals</code> / <code>!mysteries</code> chronicle lists being <strong>clipped at the right margin</strong> (e.g. <em>«The Forgotten Mysteries of the Ancient…»</em>): the in-chat content links now <strong>wrap</strong> within the card instead of overflowing. Purely presentation/robustness fixes — no behavioural change — and covered by new regression tests.</p><p>v0.11.1 — <strong>Reach Your Destination, documented and consistent</strong>. A metadata and guidance polish for the journey-finishing progress move. <em>Reach Your Destination</em> already rolled correctly against your open journey&#39;s <strong>progress score</strong>, but its move catalogue entry carried an empty stat list; it now declares <code>progress</code> like the other progress moves (<em>Fulfill Your Vow</em>, <em>End the Fight</em>), keeping the internal metadata consistent. The in-module move reference now includes a proper <em>Reach Your Destination</em> entry describing the strong-hit / weak-hit / miss outcomes, and reiterates — true to Ironsworn rules — that <strong>reaching a destination grants no experience</strong> (only fulfilling a vow awards XP). No behavioural change to rolling or completion.</p><p>v0.11.0 — <strong>End the Fight is now a real progress move</strong>. Previously the move did nothing useful when invoked; now the Skald rolls it correctly against your active combat foe&#39;s <strong>progress score</strong> (the filled progress track, not your stats). On a <strong>strong hit</strong> the foe is defeated and the fight ends, on a <strong>weak hit</strong> you win but at a cost, and on a <strong>miss</strong> the fight goes badly and the foe gains the upper hand. Completing the move clears the combat track just like Fulfill Your Vow clears a vow. True to Ironsworn rules, ending a fight grants <strong>no experience</strong> — only fulfilling vows does.</p><p>v0.10.38 — <strong>The Skald spots your asset bonuses</strong>. Ironsworn assets are free-form text with no structured bonus field, and Foundry's roll dialogs expose no API for injecting outside modifiers — so rather than fragile auto-injection, the Skald now <strong>advises</strong>. Just before a move is rolled it scans the rolling character's <strong>enabled asset abilities</strong> for roll-bonus wording (e.g. <em>“add +1”</em>, <em>“+2 when…”</em>, <em>“take +1”</em>) and, when a bonus plausibly applies to the move being made, posts a small, <strong>non-blocking chat suggestion</strong> — <em>💡 Your [Asset] grants +X — [condition]</em>. Relevance is judged by keyword overlap between the move (and the rolled stat) and the asset text, so unrelated bonuses stay quiet. The Skald <strong>never touches the roll</strong>: you decide whether to add the bonus in the official dialog, preserving full player agency and roll-system stability. A new world setting <strong>Asset Bonus Advisory</strong> (default on) toggles it. Purely additive, pattern-based (no extra AI calls), and covered by new regression tests.</p><p>v0.10.37 — <strong>The Skald brings the rulebook to the table</strong>. Phase 3 lets the Skald pull <strong>real content out of the official Ironsworn compendia</strong> and into play — always GM-gated, always through Foundry's Document API, and always verified against the compendia before anything is created. Four new effect directives: <code>[[EFFECT: create_foe Bear]]</code> spawns a full, statted <strong>foe actor</strong> copied straight from the bestiary (rank, features, tactics and progress track included) — or a minimal custom foe for a named boss flagged <code>unique</code>; <code>[[EFFECT: add_asset \"Loyal Companion\"]]</code> grants an <strong>asset</strong> to the active character (idempotent — no duplicates); <code>[[EFFECT: add_item \"Delve the Depths\"]]</code> adds any other <strong>compendium item</strong>; and <code>[[EFFECT: create_character Astrid Wolfsbane]]</code> creates a <strong>new player character</strong> with rules-legal default stats and full meters. A new world setting <strong>AI Creates Content</strong> (<em>Off</em> / <em>Foes only</em> — default / <em>Full</em>) governs it: foe-spawning is a low-risk GM convenience enabled by default, while adding assets/items and creating characters require <em>Full</em>. Names are <strong>fuzzy-matched</strong> to the catalogue; a close-but-wrong name is never guessed at — the GM is quietly advised of the nearest match instead. Every creation is whispered to the GM and audit-logged. Purely additive, conservative by default, and covered by new regression tests.</p><p>v0.10.36 — <strong>The Skald reads and tends your whole sheet</strong>. The Skald now has <strong>full character-sheet awareness</strong>: its live game state includes every <strong>stat</strong>, every <strong>meter with its maximum</strong> (e.g. <em>health 5/5</em>, <em>momentum 2/10</em>), all active <strong>impacts/conditions</strong> (including your custom debilities by their authored names), your <strong>assets with their enabled ability text</strong>, your <strong>bonds</strong>, and your <strong>XP/legacies</strong> — so it narrates grounded in who your character actually is. It can also <strong>tend the sheet from the fiction</strong> with three new GM-gated directives: <code>[[EFFECT: set_impact wounded on]]</code> / <code>[[EFFECT: clear_impact wounded]]</code> mark or clear any condition (with friendly aliases — <em>harmed→wounded</em>, <em>rattled→shaken</em>, <em>in debt→indebted</em>, …), <code>[[EFFECT: toggle_impact shaken]]</code> flips one, and <code>[[EFFECT: set_stat iron 4]]</code> safely sets a base stat (clamped 0–5). A new world setting <strong>AI Modifies Character Sheet</strong> (<em>Off</em> / <em>Impacts only</em> — default / <em>Full</em>) governs how much the Skald may change: impact edits need at least <em>Impacts only</em>, and stat edits require <em>Full</em>. Every write goes through the Foundry Document API, is bounds-checked and idempotent, audit-logged, and whispered to the GM. Purely additive, conservative by default, and fully covered by new regression tests.</p><p>v0.10.35 — <strong>Journeys split apart, foes fall properly</strong>. Two production bugs fixed. <strong>Journeys no longer group into one track</strong>: starting a journey to a <em>new</em> destination now opens its own progress track instead of advancing whatever journey was already open, so several journeys can run side by side and each tracks separately (a journey to the <em>same</em> destination still reuses its track, and a vaguely-stated trip with no named destination keeps the old conservative behaviour to avoid track spam). <strong>Journeys now complete</strong>: a journey that reaches <strong>full progress (10/10)</strong> auto-finishes and closes, which also stops the old «everything reuses the first open journey forever» loop. <strong>Foe combat auto-completes</strong>: a foe brought to <strong>10/10</strong> by Strikes/Clashes is now automatically marked defeated and the active-fight flag is cleared, and a <strong>narrated conclusion</strong> to a fight (<code>[[EFFECT: end_combat]]</code>) reliably closes the foe track even when the AI omits or paraphrases the foe's name (it falls back to a fuzzy match, then the active foe). Experience is untouched — XP is still awarded for <strong>vows only</strong>, never for completing a journey or a fight. Purely additive, deterministic, and covered by new regression tests.</p><p>v0.10.34 — <strong>The Skald understands what you DO</strong>. The move system now interprets <strong>free-form actions</strong>, not just move names. Describe what your character does — <code>!I search the ruins</code>, <code>!I attack the wolf</code>, <code>!I sneak past the guard</code> — and the Skald maps it to the right <strong>Ironsworn move</strong> and <strong>opens that roll dialog</strong>, suppressing narration until the dice resolve (then the existing strong-hit/weak-hit/miss auto-narration continues the story). It uses a <strong>hybrid classifier</strong>: the fast, deterministic v0.10.33 matcher still catches explicit move names instantly, and only genuinely free-form actions go to a small, low-temperature AI classification grounded in each move's documented <strong>trigger</strong>. <strong>Questions</strong> (\"what should I do?\") and <strong>pure roleplay</strong> (\"I tell the jarl my name\") are recognised as such and still narrate normally — they never auto-roll. <strong>Ambiguous actions</strong> (e.g. \"I attack\" → <em>Enter the Fray</em> vs <em>Strike</em> vs <em>Clash</em>) post a short <strong>confirmation card</strong> so you pick the move with one click; less-certain single matches do the same. Two new world settings: <strong>Interpret Actions Into Moves (AI)</strong> (default on; turn off to revert to explicit-name handling only) and <strong>Always Confirm Interpreted Moves</strong> (default off; require a click before any interpreted roll). Fully additive, conservative by design, and degrades gracefully to narration whenever the classifier is unavailable or unsure — keeping moves the player's choice.</p><p>v0.10.33 — <strong>Your moves are yours to make</strong>. When a player simply <strong>names an Ironsworn move</strong> in chat — <code>!Strike</code>, <code>!Face Danger</code>, <code>!I want to Secure an Advantage +iron</code> — the Skald now <strong>opens that move's own roll dialog and stops</strong>, instead of narrating on your behalf. Moves are the <strong>player's mechanical choice</strong>, so the story pauses for the dice and only resumes <em>after</em> the roll resolves (via the existing strong-hit/weak-hit/miss auto-narration). A conservative matcher recognises move names exactly (and with leading phrases like «I want to…», «let me…», «roll…», an optional <code>+stat</code>, or a short target for multi-word moves), while genuine <strong>questions and narration requests are untouched</strong> — anything with a «?» or starting with what/how/tell/describe/continue still flows to the Skald as before. Complements the existing clickable move links woven into AI narration. A new world setting <strong>Player Move Declarations Open the Dialog</strong> (default on) toggles it; purely additive and degrades gracefully when the Ironsworn system is absent.</p><p>v0.10.32 — <strong>The Skald keeps your XP ledger</strong>. Fulfilling a vow now <strong>automatically awards the correct experience</strong> for its challenge rank — <em>troublesome 1, dangerous 2, formidable 3, extreme 4, epic 5</em> — written straight to your character sheet the moment the vow is marked complete (from the sheet, an AI directive, or a <em>Fulfill Your Vow</em> strong hit). Awards are <strong>idempotent</strong>: a per-track safety flag guarantees the same vow is never counted twice, no matter how many completion paths fire. The Skald speaks <strong>both rulesets</strong> — on classic Ironsworn it increments <code>system.xp</code>; on Starforged it ticks the matching <strong>legacy</strong> track (4 ticks = 1 XP), detected purely from the system's own world flags via the Document API (no direct data mutation). The AI can also grant XP from the fiction with two new directives — <code>[[EFFECT: grant_xp &lt;amount&gt; &lt;reason&gt;]]</code> for a flat award and <code>[[EFFECT: grant_xp_vow &lt;rank&gt;]]</code> for a rank-scaled one — and every grant is whispered to the GM. Two new world settings control it: <strong>Award XP on Vow Completion</strong> (default on) and an optional <strong>Weak-Hit Half XP</strong> rule (default off, rounds up). Purely additive, GM-notified, and degrades gracefully when the system is absent.</p><p>v0.10.31 — <strong>Pick your model from a price-sorted dropdown</strong>. The <strong>AI Model</strong> setting is no longer a free-text box: it is now a <strong>dropdown that filters to your selected AI Provider</strong> and lists every vision-capable model <strong>sorted by price (free models first)</strong>, each shown as <em>Model Name ($X.XX/1M) - Provider</em>. Switch the provider and the model list updates instantly — only Abacus AI models when Abacus is selected, only OpenRouter models when OpenRouter is selected, and the matching native models for OpenAI / Google. The <strong>Vision Model</strong> setting gets the same provider-filtered, price-sorted treatment (keeping its <em>Inherit</em> option). A best-effort <strong>OpenRouter API integration</strong> refreshes the model list live from OpenRouter's public catalogue on load, falling back to an embedded list offline. Fully backwards-compatible: any custom model id you previously typed is preserved at the top of the list as <em>(current)</em>, and requests still send your chosen model verbatim. Additive only — no data-model or API changes.</p><p>v0.10.30 — <strong>Scouting no longer chokes on a dead hook</strong>. Even with the v0.10.28 fallback, <code>!scout</code> could still stall on hosted/managed Foundry: in <em>Auto</em> mode <strong>every</strong> AI call re-probed the missing <code>/skald-api</code> hook first and had to wait out a slow <strong>502 Bad Gateway</strong> from the reverse proxy before falling back to the direct browser→AI path. Because one scout fires <strong>several vision passes</strong> (a full-map overview plus each grid section), those 502 timeouts stacked up — so the scout appeared to hang or fail. The Skald now <strong>remembers</strong> the moment it first finds the hook unreachable (network error, 404, or 502/503/504) and, for the rest of the session, <strong>skips the doomed proxy POST and calls the AI directly straight away</strong>. Only the first call pays the probe cost; every later pass is fast. Purely additive and backwards-compatible — <em>Server hook only</em> mode still surfaces genuine upstream errors, and no real AI error is ever masked.</p><p>v0.10.29 — <strong>No more triple-casts</strong>. Fixed a bug where a single <code>!</code> command could be executed up to <strong>three times</strong>. Foundry hands each chat line to several interception hooks (<code>chatMessage</code> → <code>preCreateChatMessage</code> → <code>createChatMessage</code>); on builds that ignore an early cancel, the same command reached more than one hook and each ran it independently — so <code>!scout</code> could fire <strong>three AI vision passes</strong> and post duplicate cards/notices, burning 3× the tokens. The Skald now carries a short-lived (1.5s) cross-hook <strong>dedupe guard</strong>: the first hook to see a command runs it, identical re-dispatches inside the window are suppressed (the raw <code>!command</code> echo is still hidden). Purely additive and backwards-compatible.</p><p>v0.10.28 — <strong>Map vision no longer dies on a 502</strong>. Fixed the <em>MapVision 502 Bad Gateway</em> error that broke <code>!scout</code> on hosted/managed Foundry. The Skald's hosted-mode fallback only treated a <strong>404</strong> as «server hook not loaded», so when a large map-vision request hit the missing <code>/skald-api</code> route and the reverse proxy (openresty) answered with a <strong>502 Bad Gateway</strong> (or 503/504, or a 413 for an oversized body), the Skald failed instead of falling back to the direct browser→AI path — and dumped the proxy's raw HTML error page into chat. The fallback now also recognises <strong>502/503/504/413</strong> as «hook unreachable» and transparently retries the call directly, so scouting just works on hosted Foundry. Purely a resilience fix: in <em>Server hook only</em> mode a genuine upstream error is still surfaced, and no real AI error is ever masked. Backwards-compatible and additive.</p><p>v0.10.27 — <strong>The Skald keeps the saga's books</strong>. Three big upgrades to how the Skald tracks your story. <strong>Combat-foe label fix</strong>: foe tracks now render with a proper rank/progress label instead of the raw <code>IRONSWORN.ITEM.SubtypeFoe</code> key (a one-time, idempotent repair also migrates any legacy combat tracks already on your sheet). <strong>Story-arc awareness</strong>: the Skald now remembers which vow is the focus of the current arc and which foe is the active fight via lightweight actor flags, so it references the right track every time and auto-sets the focus when progress is marked. <strong>AI write directives</strong>: the Skald can now advance and conclude tracks straight from the fiction with <code>[[MARK_COMPLETE:vow:Title]]</code>, <code>[[ADD_PROGRESS:journey:Title:2]]</code> and <code>[[SET_PROGRESS:combat:Foe:5]]</code> — each fuzzy-matched to the real track on your sheet, validated, applied safely, audit-logged, and whispered to the GM. <strong>Roll integration</strong>: a Strong Hit on <em>Fulfill Your Vow</em> / <em>End the Fight</em> / <em>Reach Your Destination</em> now auto-completes the matching track (a weak hit or miss never does, and is narrated as still-in-progress). Purely additive, GM-notified, validated, and degrades gracefully.</p><p>v0.10.24 — <strong>Sharper map vision</strong>. The Skald now reads detailed fantasy maps far more thoroughly. Map captures are sent as <strong>lossless PNG at up to 4096px</strong> (no more JPEG blur on small labels), and a rewritten <strong>cartographer prompt</strong> explicitly directs the model to <strong>transcribe text labels/place names</strong>, spot <strong>small symbols and icons</strong>, trace <strong>faint paths and roads</strong>, and catalogue <strong>every settlement and structure</strong> — each with a confidence level. A new <strong>grid-sectioning</strong> analysis (the <em>Map Analysis Quality</em> setting: <em>Fast</em>, <em>Balanced</em> or <em>Thorough</em>) splits large maps into a 2×2 or 3×3 grid, runs a zoomed detail pass over each section plus a full-map overview, and intelligently combines the findings — dramatically improving recall of small text and out-of-the-way landmarks. Three new settings (<strong>Map Analysis Quality</strong>, <strong>Max Map Resolution</strong>, <strong>Map Image Format</strong>) tune the cost/quality trade-off, and the Skald now <strong>recommends strong vision models</strong> (gpt-4o, claude-3-5-sonnet, gemini-2.0-flash, gemini-2.5-pro) and whispers a GM advisory when a lightweight model (e.g. gpt-4o-mini) is likely to miss fine detail. Purely additive, GM-only, still read-only on the base map, and degrades gracefully.</p><p>v0.10.23 — <strong>Map vision (the Skald can SEE your map)</strong>. The Skald now <strong>looks at the actual background artwork</strong> of your scene with a vision-capable AI model and <strong>scouts it</strong>: it identifies <strong>terrain, landmarks, paths, hazards and points of interest</strong>, posts a saga-styled <em>Scouting</em> card, and <strong>auto-scribes each location into the Living Chronicle</strong> (linked to the scene). New scenes are scouted <strong>automatically once</strong> (cached on the scene so it never re-analyses needlessly), and you can force a fresh scout at any time with the new GM-only <code>!scout</code> command (aliases <code>!survey</code>, <code>!analyze-map</code>). The base map image is downscaled and sent as JPEG for token efficiency; only the <strong>base map</strong> is read (never tokens, drawings or fog). Two new settings — <strong>Auto-Analyze Scenes</strong> (default on) and <strong>Vision Model</strong> (inherit your narration model, or pin a known vision model) — control it. If the chosen model can't see images, the Skald skips analysis with a quiet GM notice rather than wasting a call. Purely additive, GM-only, read-only on the map, and degrades gracefully.</p><p>v0.10.22 — <strong>Map &amp; scene awareness</strong>. The Skald can now <strong>see your active map</strong>. A new read-only scene reader gathers the <strong>current scene name</strong>, its <strong>marked locations</strong> (journal-pin notes, resolved to the linked journal entry) and its <strong>notable visible tokens</strong> (hidden GM-only tokens are excluded), and folds a concise <code>CURRENT SCENE / Visible Locations / Notable Tokens</code> block into the AI context. The Skald is told it can read the map and may reference those <strong>real places by name</strong> — especially when suggesting a destination for a journey or vow — but never forces it and never invents pins that are not on the map. Purely additive, read-only (no scene manipulation), token-efficient, and degrades gracefully when no scene is active.</p><p>An AI-powered storyteller, oracle interpreter, and tactical enemy controller for <strong>Ironsworn</strong> and <strong>Ironsworn: Delve</strong> campaigns in Foundry VTT v14. Powered by Abacus AI ChatLLM. Just type <code>!</code> followed by your words to speak with the Skald, or <code>!skald-help</code> for all commands.</p><p>v0.10.21 — <strong>Reach a Milestone no longer double-marks</strong>. Triggering <em>Reach a Milestone</em> (by clicking its inline link in the narration, or via the move list) now advances your active vow by its rank <strong>exactly once</strong>. A bug caused the vow to be marked twice — once when the move was triggered and again during the follow-up narration — so the track jumped by 2× rank (or appeared inconsistent). The narration step is now told the mechanics were already applied, so it narrates the single, correct result. The milestone also recognises both Ironsworn and Starforged <code>reach_a_milestone</code> moves and works on vows you swore by hand on the sheet. Added an end-to-end regression test for the full <em>triggerMove → mark-progress</em> chain.</p><p>v0.10.19 — <strong>Bulletproofed the AI-prompt templates</strong>. Removed the last <code>\\`</code> escaped-backtick characters from the foe-prompt template literals (the word <em>unique</em> is now written with plain quotes), so the script can never be misread as having an unterminated template. Verified clean in <strong>five independent parsers</strong> — Node/V8 in both script and module mode, Acorn, Babel, and a real Chrome ES-module load. <strong>If you still see a <em>\"missing ) after argument list\"</em> error at line 1342, it is a CACHED copy of an old script</strong>, not the current code: fully update the module, then hard-refresh (<code>Ctrl+Shift+R</code>) or restart Foundry. No functional change.</p><p>v0.10.18 — <strong>Republished to clear stale caches</strong>. If the module failed to load with a JavaScript syntax error (e.g. <em>\"missing ) after argument list\"</em> around line 1342) even after updating, your browser or Foundry was still running a <strong>cached copy of the old script</strong>. This release republishes the verified-clean script (validated in strict ES-module mode) and bumps the version so Foundry re-downloads it. No functional change from v0.10.17 — if you still see the error, do a hard refresh (<code>Ctrl+Shift+R</code>) or restart Foundry.</p><p>v0.10.17 — <strong>Critical hotfix</strong>. Fixes a JavaScript <em>\"missing ) after argument list\"</em> syntax error that prevented the module from loading at all. Stray un-escaped backticks inside an AI-prompt template literal (introduced with the v0.10.14 foe catalogue) prematurely terminated the string when Foundry loaded the script as an ES module. All scripts now validate in strict module mode.</p><p>v0.10.16 — <strong>Clean slate for a new campaign</strong>. A new GM-only <code>!skald-reset</code> command (alias <code>!skald-wipe</code>) wipes the chronicle so you can start a fresh saga: after a confirmation dialog it <strong>deletes all unlocked Skald-scribed journal entries</strong>, clears the <strong>semantic memory (RAG) vectors</strong>, resets the <strong>conversation history</strong>, and empties the <strong>campaign timeline</strong> — then whispers a report of exactly what was cleared. Your own (non-Skald) journals are never touched, and any entry you lock (its <code>the-eternal-skald.locked</code> flag set to <code>true</code>) is preserved.</p><p>v0.10.15 — <strong>Journeys that just work</strong>. Rolling <em>Undertake a Journey</em> now <strong>automatically opens a journey progress track</strong> (named for your destination) when none is open, and marks progress on a hit — so <em>Reach Your Destination</em> always has a track to roll against instead of failing with «No open journey track…». The Skald can also <strong>advance or complete a specific vow/journey by its exact title from the fiction</strong> — a new <code>[[EFFECT: mark_progress \"Your Vow Title\"]]</code> directive marks the named track, and narrative completion no longer requires a roll first. Your open vows and journeys are now listed <strong>by title</strong> in the AI's live game state, so the Skald references the right track every time.</p><p>v0.10.14 — <strong>Real foes from the official compendia</strong>. The Skald now picks <strong>regular foes from the official foundry-ironsworn foe compendia</strong> (<em>Ironsworn Foes</em> + <em>Delve Foes</em>) instead of inventing names. The full catalogue of available foes (grouped by rank) is embedded in the AI prompt, and the Skald is instructed to draw routine encounters from that list <em>verbatim</em> so ranks come straight from the rulebook. <strong>Important narrative foes</strong> — a named boss or unique antagonist the story is built around — may still be <strong>custom-created</strong>; the Skald marks them with a <code>unique</code> keyword so they are sized intentionally. A GM-only advisory quietly flags when a routine foe isn't an official compendium foe. The foe list is loaded and cached on world load.</p><p>v0.10.13 — <strong>Vow tracking reads straight from your sheet</strong>. Fixes a bug where the Skald's vow card could show a <em>phantom</em> vow (e.g. \"Vow\", rank 1, 0/10 boxes, marked complete) that had nothing to do with the real vow on your character sheet. The Skald now resolves and displays vows/journeys/tracks <strong>directly from the foundry-ironsworn items on your sheet</strong>, read fresh every time — progress comes from <code>system.current</code> (ticks ÷ 4 = boxes) and completion from <code>system.completed</code>, so the card always mirrors the sheet. Clicking the bare word \"vow\" now opens your <strong>actual current vow</strong> (e.g. \"The Truth of the Star-Fall\") instead of a generic phantom, and marking progress writes back to the sheet immediately. Generic nouns like \"vow\"/\"journey\" are no longer turned into misleading phantom links.</p><p>v0.10.12 — <strong>Works on hosted Foundry (direct browser→AI fallback)</strong>. Fixes the runtime <code>/skald-api/chat 404 (Not Found)</code> error that stopped the Skald from reaching the AI on hosted/managed Foundry (e.g. <em>Foundry VTT on Abacus</em>), where you can't launch Foundry with the server-side <code>--import</code> flag the server hook needs. The Skald now <strong>automatically falls back to calling the AI directly from your browser</strong> when the server hook isn't loaded, so it just works out of the box. A new <strong>Connection Mode</strong> setting (<em>Auto</em> — default, <em>Server hook only</em>, or <em>Direct browser→AI</em>) lets you control this. The default Abacus AI endpoint allows the cross-origin request, so no setup is required.</p><p>v0.10.11 — <strong>Vow &amp; journey completion fix</strong>. Fulfilling a vow or reaching a destination now closes the <strong>correct track</strong>. Previously, completing a quest after a <em>Fulfill Your Vow</em> / <em>Reach Your Destination</em> roll could fail with «Track \"Reach Your Destination\" not found», because the system looked for a track named after the <em>move</em> instead of your real, player-named vow/journey. The Skald now remembers which track a progress move actually rolled against and closes that one — and if the name is uncertain or omitted, it falls back to the most recent open vow/journey of the right kind.</p><p>v0.10.10 — <strong>Move suggestions woven into the prose</strong>. Suggested moves are no longer shown as separate <em>A Move Beckons</em> / <em>What Comes Next</em> cards. Instead the Skald names the fitting move <strong>inside its narration</strong>, where it appears as a subtle <strong>clickable link woven into the story</strong> — both in ordinary narration (<code>!skald</code>, <code>!scene</code>, <code>!combat</code>) and in the <strong>post-roll outcome narration</strong> after a move resolves. Clicking the link rolls the move through the same progress-aware path as before (so progress moves like <em>Reach Your Destination</em> / <em>Fulfill Your Vow</em> roll against their track). No more standalone suggestion bubbles cluttering the chat.</p><p>v0.10.9 — <strong>Faithful progress-track integration</strong>. After studying the live <strong>foundry-ironsworn</strong> source, the Skald now creates vows, journeys, bonds and combat foes exactly as the system itself does: a single Item type <code>progress</code> distinguished by <code>system.subtype</code> (<em>vow</em> / <em>progress</em> / <em>bond</em> / <em>foe</em>), with proper numeric challenge ranks and tick-based progress. A stray, non-schema <code>notes</code> field was removed, the standard sort order is applied, and combat foes now use the system's real <em>foe</em> subtype. The Skald also recognises tracks you create by hand on the sheet — sworn vows, journeys and foes — so progress, completion and the <em>Fulfill Your Vow</em> / <em>Reach Your Destination</em> rolls work on them too.</p><p>v0.10.8 — <strong>Inline move suggestions return</strong>. Move suggestions are once again woven as <strong>clickable links inside the narration</strong> (reverting the separate <em>A Move Beckons</em> / <em>What Comes Next</em> button-cards). Clicking an inline move link now routes through the progress-aware trigger, so <strong>progress moves like <em>Reach Your Destination</em> roll correctly</strong> instead of failing with \"no dialog and no rollable stat\". Journey-track lookup gained a <strong>fallback</strong> that finds legacy or hand-made journey tracks (older <code>progress</code>-subtype tracks without the journey flag). Finally, <strong>vow / journey creation now surfaces failures</strong> — if there is no active character or the data is rejected, the Skald whispers the GM exactly why instead of failing silently, and track ranks are written as proper numeric challenge ranks.</p><p>v0.10.4 — <strong>Manual-roll narration fix</strong>. When you roll a move from the Skald's <strong>What Comes Next</strong> buttons (or any manual fallback roll), the Skald now reliably detects the result and continues the story. Previously these rolls — posted as the module's own manual-roll cards — were skipped by the chat hook and never narrated, breaking the narrative flow.</p><p>v0.10.3 — <strong>Combat track cleanup</strong>. Ironsworn is fought one foe at a time, so when a new fight begins the Skald now automatically closes any combat track left open from a previous fight — no more orphaned, untracked foe tracks lingering on your sheet. Several foes introduced in the <em>same</em> scene still coexist as separate tracks, and a new <strong>Auto-Close Old Combat Tracks</strong> world setting (default on) lets you opt out for simultaneous fights.</p><p>v0.10.2 — <strong>Journey completion</strong>. Journeys are now first-class progress tracks just like vows: the Skald can begin one with <code>create_journey</code> and close it with <code>complete_journey</code>, and progress-track cards show a manual <strong>Reach Destination (mark complete)</strong> button for journeys (works even with AI-applied effects disabled). Journeys are now identified by their <code>journey</code> subtype and journey-flavoured throughout the UI.</p><p>v0.10.1 — <strong>Vow completion fix</strong>. Vows (and other progress tracks) can now actually be marked complete. A new <code>complete_vow</code> AI effect (plus synonyms) lets the Skald close out a fulfilled vow, and a manual <strong>Mark Complete / Fulfill Vow</strong> button on progress-track cards lets you fulfil a vow yourself at any progress level (works even with AI-applied effects disabled). Also fixes vow identification to match the modern foundry-ironsworn data model.</p><p>v0.10.0 — <strong>Follow-up move suggestions</strong>. After narrating a resolved move, the Skald now offers a <strong>What Comes Next</strong> card with two one-click follow-up moves to roll next (plus <em>Roll Any Other Move</em>), mirroring the pre-roll suggestion card. The Skald may no longer invent moves — every suggested move is validated against the real Ironsworn / Starforged move catalogue.</p><p>v0.9.3 — <strong>Abacus AI endpoint fix</strong>. Corrected the Abacus AI provider/default endpoint to the working <code>https://routellm.abacus.ai/v1/chat/completions</code> (the v0.9.2 default, <code>api.abacus.ai/v0</code>, was non-functional). Existing worlds still pinned to the bad URL are <strong>auto-migrated</strong> on load — no manual change needed. Backwards-compatible; you still supply your own API key and model name.</p><p>v0.9.2 — <strong>Abacus AI by default</strong>. The <strong>AI Provider</strong> dropdown now includes <strong>Abacus AI</strong> (the engine that powers the Skald) as the recommended, default option, pointing at the Abacus AI chat-completions endpoint out of the box. The full provider order is now <strong>Abacus AI</strong> (default), <strong>OpenAI</strong>, <strong>OpenRouter</strong>, <strong>Google AI (Gemini)</strong> and <strong>Custom</strong>. Additive and backwards-compatible — you still supply your own API key and model name.</p><p>v0.9.1 — <strong>multi-provider made easy</strong>. A new <strong>AI Provider</strong> dropdown in Module Settings lets you switch between providers in one click — picking a provider auto-fills the API Endpoint with its OpenAI-compatible URL (you still supply your own API key and model name). Backwards-compatible and additive.</p><p>v0.9.0 — the <strong>Attentive Chronicle</strong>. The Skald now reads the room: <strong>context-aware suggestions</strong> weave subtle, situation-aware next-step hints from the active scene, location and recent story threads into its narration (never forcing the story). <strong>Lore contradiction alerts</strong> quietly compare each freshly recorded fact against established chronicle lore and whisper a GM-only advisory when the saga seems to trip over itself — nothing is ever changed automatically. <strong>Idle auto-summaries</strong> let a lull in play weave a Session Chronicle on its own, so a recap is never lost when <code>!end-session</code> is forgotten. <strong>Customizable link styles</strong> let you recolor and re-icon in-chat entity links to taste via the new <code>!link-style</code> command. Under the hood, <strong>entity indexing is optimized</strong> with a generation-keyed journal cache, so linking stays snappy across campaigns of 100+ journals. All additive, backwards-compatible, and degrade gracefully. Still an early (v0.x) alpha — expect rough edges.</p><p>v0.8.0 — the <strong>Living Chronicle, deepened</strong>. The chronicle now maps <strong>relationships</strong> between the folk and places it records: the Skald reads the bonds in its narration, links them bidirectionally as journal content-links, and shows a <em>Connections</em> section inside each entry — review the whole web with the new <code>!relationships</code> (alias <code>!map</code>) command. A persistent <strong>Timeline</strong> remembers every beat of the saga across sessions and reloads; browse or filter it with <code>!timeline</code>. NPCs, locations and discoveries gain <strong>structured template fields</strong> (rank, harm, goals, region, dangers, resources and more) that the AI fills in and you can author by hand with the new <code>!template</code> dialog. Finally, <strong>smarter entity detection</strong> recognises aliases and name variations (\"the captain\" → <em>Captain Reeves</em>) so the chronicle stops creating duplicates and links every mention. Purely additive, backwards-compatible with existing journals, and degrades gracefully.</p><p>v0.7.0 — <strong>Entity linking expansion</strong>. The Skald now links even more of what it speaks, directly in chat. <strong>Oracle</strong> names become one-click links that roll that very oracle (e.g. <em>Action Oracle</em>), <strong>Progress Tracks</strong> from your active character become links that open a status card with a one-click <em>Mark Progress (by rank)</em> button, and <strong>Assets</strong> become links that open the asset's own sheet (with a graceful chat-card fallback). Asset matching uses fuzzy lookup across your compendia, and the entity index now refreshes automatically as items, actors and tokens change — so links stay current mid-session. Builds on the v0.6.0 clickable NPCs, locations and moves; purely additive and degrades gracefully.</p><p>v0.6.0 — <strong>Clickable entities in narration</strong>. Names the Skald speaks are now linked inline in chat: chronicled NPCs, locations and discoveries become Foundry content links that open their Journal Entry, and known Ironsworn moves become one-click links that open the <strong>foundry-ironsworn system's own official move dialog</strong> directly (resolved by the move's Datasworn ID). A new world setting <em>Link Entities in Narration</em> (default on) toggles the feature; it is purely additive and degrades gracefully.</p><p>v0.5.0 — <strong>AI Memory</strong>. The Skald now remembers your whole saga. Every chronicle entry is embedded into a private, in-browser semantic memory (Browser-Based RAG) and the most <em>relevant</em> NPCs, locations, facts and threads are recalled automatically before the Skald speaks — so continuity holds across sessions without bloating the prompt. It runs entirely in your browser (no server, no cloud, no API keys for memory); a small model downloads once and is cached. <code>!remind</code> is now semantic, and two new commands — <code>!reindex</code> (rebuild memory) and <code>!rag-status</code> (inspect it) — join six new settings. Memory degrades gracefully: the AI works fully even when it can't load.</p><p>v0.4.0 — the <strong>Living Chronicle</strong>. The Skald now <strong>automatically scribes your saga into Foundry Journal Entries</strong> as it unfolds: NPCs, locations and discoveries each get their own entry, while world facts and story threads accumulate in rolling journals — all neatly organized into folders under <em>The Eternal Skald</em>. Journaling runs quietly in the background and never interrupts narration. New commands let you review your world: <code>!journals</code> lists what's been recorded, <code>!mysteries</code> surfaces open threads, <code>!remind</code> recalls what the chronicle knows about a topic, and <code>!end-session</code> weaves a saga-styled session recap. Four new settings control auto-journaling, notification verbosity, player visibility, and session summaries. Builds on the v0.3.3 real-time streaming, the v0.3.2 bare <code>!</code> alias and AI Mode toggle, v0.3.1 compendium enemy ranks, and the v0.3.0 automatic combat system, on top of deep integration with the <strong>foundry-ironsworn</strong> rules engine. Works standalone too — Ironsworn features activate automatically when the system is present.</p>",
  "version": "0.13.0",
  "compatibility": {
    "minimum": "13",
    "verified": "14",
    "maximum": "14"
  },
  "authors": [
    {
      "name": "The Eternal Skald Project"
    }
  ],
  "esmodules": [
    "scripts/eternal-skald.js"
  ],
  "styles": [
    "styles/eternal-skald.css"
  ],
  "languages": [
    {
      "lang": "en",
      "name": "English",
      "path": "lang/en.json"
    }
  ],
  "relationships": {
    "recommends": [
      {
        "id": "foundry-ironsworn",
        "type": "system",
        "reason": "Optional but recommended. When the Ironsworn system is active, the Skald integrates with its rules engine to trigger moves, read character state, and narrate official roll outcomes. The module also works standalone in any system, but Ironsworn-specific mechanics are then unavailable."
      }
    ]
  },
  "url": "https://github.com/papicy/the_eternal_skald",
  "manifest": "https://raw.githubusercontent.com/papicy/the_eternal_skald/main/module.json",
  "download": "https://github.com/papicy/the_eternal_skald/archive/refs/heads/main.zip",
  "readme": "https://github.com/papicy/the_eternal_skald/blob/main/README.md"
}
